#include <bits/stdc++.h>
using namespace std;

int main()
{
    // Multimap Set - Not Unique (Multiple element) and sorted order - O(logn)
    multimap<int, int> mp;
    // only mp[key] cannot be used here
    //  lower and upper bound not works
    //  Work rest of funations are same
    //

    return 0;
}